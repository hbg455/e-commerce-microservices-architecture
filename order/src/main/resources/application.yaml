spring:
  cloud:
    function:
      definition: orderSupplier;stockEventConsumer
    stream:
      bindings:
        orderSupplier-out-0:
          destination: order-event
          group: order-group
        stockEventConsumer-in-0:
          destination: stock-event
          group: stock-group